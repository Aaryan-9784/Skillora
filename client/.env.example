# Backend API base URL (Vite proxies /api to http://localhost:5000 in dev)
VITE_API_URL=/api

# Backend Server URL (used for Socket.io real-time events and OAuth redirects)
VITE_SERVER_URL=http://localhost:5000
